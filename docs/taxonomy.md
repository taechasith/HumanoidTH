# Taxonomy

Canonical taxonomy lives in `data/taxonomy.yml`. Labels are intentionally conservative and include `unknown` or `unclear` when evidence is thin.

Perspective labels describe observed public/media signals from collected records. They must not be presented as complete Thai public opinion.
